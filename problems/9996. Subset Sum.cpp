int subsetSum(vector<int> sizes, int n, int C) {
    vector<vector<int>> dp(n + 1, vector<int>(sizes.size() + 1, 0));

    for (int i = 0; i < n + 1; i++) {
        for (int i = 0; i < C; i++) {
        }
    }
}


int main(int argc, char** argv) {
    return 0;
}
